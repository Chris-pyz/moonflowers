index

<% @users.each do |user|%>
<li>nickname: <%= link_to user.nickname, user_path(user.id) %>
</li>
  <% if user == current_user %>
  <%= link_to "update profil", edit_user_path(user.id) %>
  <% end %>
<hr>
<%end %>

<%= link_to "home", root_path %>


<%# id => faire lien en savoir + pour self-description jvs %>
